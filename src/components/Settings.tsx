import { useNavigationStore } from '../stores/navigationStore';
export function Settings({open,onClose}:{open:boolean;onClose:()=>void}){const s=useNavigationStore();if(!open)return null;return <aside className="settings panel"><header><b>SETTINGS</b><button onClick={onClose}>×</button></header><h4>GRAPHICS</h4>{(['atmosphere','stars'] as const).map(k=><label className="switch" key={k}>{k.toUpperCase()}<input type="checkbox" checked={s.settings[k]} onChange={()=>s.toggle(k)}/><span/></label>)}<h4>NAVIGATION</h4><label className="switch">AUTO ROTATE<input type="checkbox" checked={s.settings.autoRotate} onChange={()=>s.toggle('autoRotate')}/><span/></label><label className="switch">IMPERIAL UNITS<input type="checkbox" checked={s.settings.units==='imperial'} onChange={()=>s.toggle('units')}/><span/></label><h4>DATASETS</h4><ul className="datasets"><li>Cities & capitals <b>READY</b></li><li>Countries <b>READY</b></li><li>Airports <b>READY</b></li><li>Procedural Earth materials <b>READY</b></li><li>Road network <em>{s.roadData.status==='READY'?(s.roadData.type==='demo'?'DEMO DATA':'OSM DATA'):s.roadData.status}</em></li>{s.roadData.status==='READY'&&<li>Graph <b>{s.roadData.nodeCount} N / {s.roadData.edgeCount} E</b></li>}</ul></aside>}
