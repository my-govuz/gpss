import { describe,it,expect } from 'vitest'; import { bearing,destinationPoint,distanceKm,interpolate } from './geodesic'; import { parseCoordinates,searchPlaces } from '../geocoding/geocoder';
const a={id:'a',name:'a',country:'',kind:'selected' as const,lat:0,lon:0}, b={...a,id:'b',lat:0,lon:1};
describe('offline geodesy',()=>{it('calculates equatorial distance and bearing',()=>{expect(distanceKm(a,b)).toBeCloseTo(111.195,2);expect(bearing(a,b)).toBeCloseTo(90,5)});it('calculates a destination point',()=>expect(destinationPoint(a,90,111.195).lon).toBeCloseTo(1,2));it('interpolates over the antimeridian',()=>{const p=interpolate({...a,lon:179},{...b,lon:-179},.5);expect(Math.abs(p.lon)).toBeGreaterThan(179)});it('rejects invalid coordinates',()=>expect(parseCoordinates('91, 0')).toBeUndefined())});
describe('local geocoder',()=>{for(const query of ['Ташкент','ташкент','Tashkent','Toshkent','Toshkent shahri','Tash','Tashknet'])it(`finds ${query}`,()=>expect(searchPlaces(query)[0]?.id).toBe('tashkent'));it('parses coordinates',()=>expect(parseCoordinates('41.3111, 69.2797')?.lat).toBe(41.3111));it('returns no remote fallback',()=>expect(searchPlaces('an impossible place')).toEqual([]))});
